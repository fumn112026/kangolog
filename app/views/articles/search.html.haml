.content
  %h2.index__head
    = "#{@articles.count}件の検索結果"
    .index__inner
      - if @articles.present?
        %ul
          = render @articles
      - else
        %p.article__search__noarticle
          = "「#{params[:keyword]}」に一致する記事は見つかりませんでした。"
    = paginate(@articles)


