%h2 記事検索
= form_with(url: search_articles_path, local: true, method: :get) do |f|
  = f.text_field :keyword, placeholder: '記事を検索する'
  = f.submit '検索'

%h2 記事検索結果
= render @articles

= link_to 'トップページへ戻る', root_path
