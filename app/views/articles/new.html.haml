新規投稿ページ

= form_with model: @article, local: true do |f|
  = f.text_field :title
  = f.fields_for :images do |i|
    = i.file_field :src
  = f.text_area :content
  = f.submit '投稿する'

= link_to 'トップページに戻る', root_path
